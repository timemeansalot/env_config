{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "codelldb",
      "request": "launch",
      "name": "Debug C++ Main",
      "program": "${workspaceFolder}/main{{_cursor_}}",
      "args": ["hello", "world"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build cpp debug"
    }
  ]
}
